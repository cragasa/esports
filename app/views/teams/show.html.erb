<h2><%= @team.name %></h2>

<p>Total Players: <%= @team.players.size %></p>
<p>Total Coaches: <%= @team.coaches.size %></p>

<h3>All Players</h3>
<ul class="list-group">
  <% @team.players.each do |player| %>
    <li class="list-group-item"><%= link_to player.name, player %> also known as <%= player.game_name %> from <%= player.country %></li>
  <% end %>
</ul>

<h3>All Coaches</h3>
<ul class="list-group">
  <% @team.coaches.each do |coach| %>
    <li class="list-group-item"><%= link_to coach.name, coach %></li>
  <% end %>
</ul>

<p><%= link_to 'Home', root_path %></p>