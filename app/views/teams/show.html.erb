<h2><%= @team.name %></h2>

<p>Total Players: <%= @team.players.size %></p>
<p>Total Coaches: <%= @team.coaches.size %></p>

<h3>All Players</h3>
<ul>
  <% @team.players.each do |player| %>
    <li><%= link_to player.name, player %> also known as <%= player.game_name %> from <%= player.country %></li>
  <% end %>
</ul>

<h3>All Coaches</h3>
<ul>
  <% @team.coaches.each do |coach| %>
    <li><%= link_to coach.name, coach %></li>
  <% end %>
</ul>

<p><%= link_to 'Home', root_path %></p>